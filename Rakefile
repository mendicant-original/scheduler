# -*- ruby -*-

require 'rubygems'
require 'hoe'

Hoe.spec 'scheduler' do
  # self.rubyforge_name = 'schedulerx' # if different than 'scheduler'
  developer('Gordon Thiesfeld', 'gthiesfeld@gmail.com')
  self.readme_file = 'README.rdoc'
  extra_deps << ['runt', '= 0.7.0']
end

# vim: syntax=ruby
